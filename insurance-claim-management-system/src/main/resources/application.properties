spring.application.name=insurance-claim-management-system
# Server
server.port=8080

# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-damhngqjnfac73anhsp0-a.singapore-postgres.render.com:5432/icms_db_2w5y?sslmode=require}
spring.datasource.username=${DB_USERNAME:icms_db_2w5y_user}
spring.datasource.password=${DB_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=3600000